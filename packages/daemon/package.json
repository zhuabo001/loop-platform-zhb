{
  "name": "@loopzhb/daemon",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Loop-platform-zhb machine daemon: short-poll claim → sandboxed Claude Code runner → report, with in-process retry of unconfirmed reports. The production runner is the real Claude Code adapter (fail-closed OS sandbox, probe-pinned binary — ADR-006); the Fake Runner survives only as a test fixture. Production dependency surface is @loopzhb/protocol only; HTTP is Node 22 native fetch.",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "pnpm --filter @loopzhb/protocol build && pnpm build:wrapper && tsc -p tsconfig.build.json && pnpm check:wrapper",
    "build:wrapper": "node scripts/build-wrapper.mjs",
    "check:wrapper": "node scripts/build-wrapper.mjs --check",
    "start": "node --enable-source-maps dist/cli.js",
    "typecheck": "pnpm --filter @loopzhb/protocol build && pnpm build:wrapper && tsc --noEmit",
    "test": "pnpm --filter @loopzhb/protocol build && pnpm build:wrapper && pnpm check:wrapper && vitest run"
  },
  "dependencies": {
    "@loopzhb/protocol": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^25.9.3",
    "esbuild": "0.28.1",
    "typescript": "^6.0.3",
    "vitest": "^4.1.9"
  }
}
